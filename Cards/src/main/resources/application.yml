server:
  port: 9000
spring:
  application:
    name: Cards
  datasource:
    url: ${SPRING_DATASORCE_URL}
    username: ${SPRING_DATASORCE_USERNAME}
    password: ${SPRING_DATASORCE_PASSWORD}
  jpa:
    show-sql: true
  sql:
    init:
      mode: always